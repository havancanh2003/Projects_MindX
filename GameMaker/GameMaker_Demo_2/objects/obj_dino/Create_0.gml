// obj_dino - Create Event
jump_speed = -10;  // Tốc độ nhảy (âm để nhân vật nhảy lên)
gravity = 0.5;     // Trọng lực (dương để kéo nhân vật xuống)
vsp = 0;           // Vận tốc theo chiều dọc
on_ground = true;  // Biến kiểm tra nếu nhân vật đang đứng trên mặt đất
speed = 5;         // Tốc độ di chuyển (trái qua phải)
global.score = 0;          // Điểm số bắt đầu



